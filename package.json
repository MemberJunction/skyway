{
  "name": "skyway-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli",
    "clean": "npm run clean --workspaces",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
